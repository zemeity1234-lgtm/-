name: Global Nuclear Missile V2
on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - name: Global Fetching
        uses: actions/checkout@v3

      - name: AI Python Setup
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Nuclear Speed Dependencies
        run: |
          sudo apt-get update --fix-missing
          sudo apt-get install -y git zip unzip openjdk-17-jdk autoconf libtool pkg-config zlib1g-dev libncurses5-dev libncursesw5-dev cmake libffi-dev libssl-dev || true
          pip install --upgrade pip
          pip install Cython==0.29.33 buildozer

      - name: Stable APK Production
        run: |
          # Ø¥Ø¶Ø§ÙØ© ÙÙÙÙ Ø°ÙÙ ÙÙØ£ÙØ§ÙØ± ÙÙÙØ¹ Ø§ÙÙØ·Ø§Ø¹ Ø§ÙØ§ØªØµØ§Ù (Broken Pipe)
          export LC_ALL=C.UTF-8
          yes | buildozer android debug || (sleep 
