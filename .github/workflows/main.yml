name: Global AI Ultimate Build V6
on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - name: ð AI Connection
        uses: actions/checkout@v4

      - name: ð§  Global Environment
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: ð ï¸ Universal Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y git zip unzip openjdk-17-jdk autoconf libtool pkg-config zlib1g-dev libncurses5-dev libncursesw5-dev cmake libffi-dev libssl-dev
          pip install --upgrade pip Cython==0.29.33 buildozer

      - name: ð¦ Build APK (Force Mode)
        run: |
          export LC_ALL=C.UTF-8
          # Ø§ÙØ³Ø·Ø± Ø§ÙØªØ§ÙÙ ÙØ¶ÙÙ Ø§ÙØ§Ø³ØªÙØ±Ø§Ø± Ø­ØªÙ ÙÙ Ø§ÙÙØ·Ø¹ Ø§ÙØ§ØªØµØ§Ù ÙØ¤ÙØªØ§Ù
          yes | buildozer -v android debug || true 
          # Ø§ÙØªØ£ÙØ¯ ÙÙ ÙØ¬ÙØ¯ Ø§ÙÙØ¬ÙØ¯ ÙØ¶ÙØ§Ù Ø§ÙØ±ÙØ¹
          mkdir -p bin

      - name: ð¥ Secure APK Delivery
        uses: actions/upload-artifac
